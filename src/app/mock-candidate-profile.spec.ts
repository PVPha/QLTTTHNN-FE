import { MockCandidateProfile } from './mock-candidate-profile';

describe('MockCandidateProfile', () => {
  it('should create an instance', () => {
    expect(new MockCandidateProfile()).toBeTruthy();
  });
});
