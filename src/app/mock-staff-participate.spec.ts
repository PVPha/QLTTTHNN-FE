import { MockStaffParticipate } from './mock-staff-participate';

describe('MockStaffParticipate', () => {
  it('should create an instance', () => {
    expect(new MockStaffParticipate()).toBeTruthy();
  });
});
