import { MockSalaryAccordingToSale } from './mock-salary-according-to-sale';

describe('MockSalaryAccordingToSale', () => {
  it('should create an instance', () => {
    expect(new MockSalaryAccordingToSale()).toBeTruthy();
  });
});
