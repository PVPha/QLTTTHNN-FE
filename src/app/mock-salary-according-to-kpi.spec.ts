import { MockSalaryAccordingToKpi } from './mock-salary-according-to-kpi';

describe('MockSalaryAccordingToKpi', () => {
  it('should create an instance', () => {
    expect(new MockSalaryAccordingToKpi()).toBeTruthy();
  });
});
