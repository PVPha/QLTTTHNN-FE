import { MockTimekeepingAccordingToSale } from './mock-timekeeping-according-to-sale';

describe('MockTimekeepingAccordingToSale', () => {
  it('should create an instance', () => {
    expect(new MockTimekeepingAccordingToSale()).toBeTruthy();
  });
});
