import { MockTimekeepingAccordingToKPI } from './mock-timekeeping-according-to-kpi';

describe('MockTimekeepingAccordingToKPI', () => {
  it('should create an instance', () => {
    expect(new MockTimekeepingAccordingToKPI()).toBeTruthy();
  });
});
